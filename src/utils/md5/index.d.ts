/**
 * 计算字符串的 MD5 哈希值，并返回十六进制字符串。
 * @param str 要计算哈希值的字符串。
 * @returns 字符串的 MD5 哈希值的十六进制表示。
 */
export function hexMD5(str: string): string

/**
 * 计算宽字符字符串的 MD5 哈希值，并返回十六进制字符串。
 * @param str 要计算哈希值的宽字符字符串。
 * @returns 宽字符字符串的 MD5 哈希值的十六进制表示。
 */
export function hexMD5w(str: string): string

/**
 * 计算字符串的 MD5 哈希值，并返回 Base64 编码的字符串。
 * @param str 要计算哈希值的字符串。
 * @returns 字符串的 MD5 哈希值的 Base64 编码表示。
 */
export function b64MD5(str: string): string

/**
 * 计算宽字符字符串的 MD5 哈希值，并返回 Base64 编码的字符串。
 * @param str 要计算哈希值的宽字符字符串。
 * @returns 宽字符字符串的 MD5 哈希值的 Base64 编码表示。
 */
export function b64MD5w(str: string): string

/**
 * 计算字符串的 MD5 哈希值，并返回十六进制字符串。
 * @param str 要计算哈希值的字符串。
 * @returns 字符串的 MD5 哈希值的十六进制表示。
 */
export function calcMD5(str: string): string
